#include "kris_embed_os.h"
#include "common.h"

int main(void) {
	
	
	OS_init();
	
	
	/*
	int32_t index;
	int32_t *array;
	int8_t *array2;
	
	array = (int32_t*) malloc(1 * sizeof(int32_t));
	array2 = (int8_t*) malloc(13 * sizeof(int8_t));
	
	*array = 0xFFFF;
	
	for (index = 0; index < 13; index++) 
		array2[index] = 0x11;
	
	free(array);
	free(array2);
	*/

	while(1);
}
