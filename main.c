#include "cortex_m4.h"

int main(void) {
	
	
	/*
	int32_t index;
	int32_t *array;
	int8_t *array2;
	
	array = (int32_t*) heap_allocate(1 * sizeof(int32_t));
	array2 = (int8_t*) heap_allocate(13 * sizeof(int8_t));
	
	*array = 0xFFFF;
	
	for (index = 0; index < 13; index++) 
		array2[index] = 0x11;
	
	heap_free(array);
	heap_free(array2);
	
	__disable_irqs();
	__enable_irqs();
	__set_base_prio(2);
	
	*/
	while(1);
}
